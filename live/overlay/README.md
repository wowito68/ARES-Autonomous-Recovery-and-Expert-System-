# Overlay

La raíz inferior es SquashFS/verity. La capa superior normal es tmpfs y no se versiona aquí. Este directorio documenta el contrato; el overlay de archivos incorporados al root vive en `config/includes.chroot/` según la convención de live-build.
