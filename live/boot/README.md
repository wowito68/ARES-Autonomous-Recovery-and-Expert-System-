# Boot

Las plantillas ejecutables viven en `live/config/bootloaders/`: SYSLINUX/ISOLINUX para BIOS y GRUB para UEFI. Los perfiles Live, persistente, forense, recuperación y gráficos seguros deben existir en ambos menús y se validan automáticamente.
