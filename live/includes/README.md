# Includes

`live/config/includes.chroot/` es el overlay de transición para Fase 2; `includes.binary/` contiene solo datos visibles en el medio. Configuración productiva migrará a `ares-live-config.deb` para conservar ownership de dpkg y upgrades auditables.
