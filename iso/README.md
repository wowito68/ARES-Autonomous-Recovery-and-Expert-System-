# Artefactos ARES OS

`make build-iso` escribe aquí `ARES.iso`, checksum, manifest y lista de paquetes. Los artefactos generados están ignorados por Git; este README conserva el contrato del directorio.
